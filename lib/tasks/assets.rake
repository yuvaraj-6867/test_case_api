namespace :assets do
  desc 'Precompile assets (no-op for API-only app)'
  task :precompile do
    # No-op for API-only Rails app
  end

  desc 'Clean assets (no-op for API-only app)'
  task :clean do
    # No-op for API-only Rails app
  end
end